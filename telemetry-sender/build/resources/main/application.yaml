spring:
  application:
    name: telemetry-sender
logging:
  config: classpath:log4j2.yaml

management:
  endpoints:
    web:
      exposure:
        include: "*"
otel:
  exporter:
    otlp:
      endpoint: http://localhost:4317
  resource:
    attributes: service.name=springboot-service


